version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cp public/dagens.html public/index.html
    build:
      commands:
        - echo "Static landing page ready"
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
